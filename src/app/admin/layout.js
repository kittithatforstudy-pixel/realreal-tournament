export const metadata = {
  title: 'Admin Panel',
  description: 'จัดการทัวร์นาเมนต์ สลิป และเกม',
  robots: { index: false, follow: false }
}

export default function AdminLayout({ children }) {
  return children
}
